defaults: [../experiment/wallgap]
name: baseline
policies:
  checkpoint_selection: last
representation_collection:
  dataset_artifact_id: auto
  split_artifact_id: auto
  split_names: [train, validation, test]
  sources: [encoder.place_codes]
  max_episodes: 512
  device: auto
