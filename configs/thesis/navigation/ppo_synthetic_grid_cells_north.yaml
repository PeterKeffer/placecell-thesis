defaults: [../../downstream/wallgap_navigation_ppo]
name: ppo_synthetic_grid_cells_north
models:
  vision_encoder_artifact_id: auto
observation:
  feature_sources: [synthetic_grid_cells, goal_xy_scaled, heading_sin_cos]
  synthetic_grid_cells:
    normalization: l2
training:
  total_timesteps: 3000000
  learning_rate: 0.0001
  ent_coef: 0.02
