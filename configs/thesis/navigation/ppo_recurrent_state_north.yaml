defaults: [../../downstream/wallgap_navigation_ppo]
name: ppo_recurrent_state_north
models:
  place_model_artifact_id: tag:baseline
  place_representation_source: encoder.hidden_state
training:
  learning_rate: 0.0001
  ent_coef: 0.02
