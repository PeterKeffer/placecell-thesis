defaults: [../../downstream/wallgap_navigation_dqn]
name: dqn_pixels_north
observation:
  mode: raw_pixels
  feature_sources: [goal_xy_scaled, heading_sin_cos]
training:
  cnn_features_dim: 512
  device: auto
  exploration_final_eps: 0.05
