defaults: [../../downstream/wallgap_navigation_ppo]
name: ppo_place_code_north
models:
  vision_encoder_artifact_id: auto
  place_model_artifact_id: tag:baseline
goal_task:
  candidate_positions_xy:
  - [0.0, 18.0]
observation:
  feature_sources: [place_codes_l2, goal_xy_scaled, heading_sin_cos]
training:
  total_timesteps: 3000000
  learning_rate: 0.0005
  ent_coef: 0.01
tracking:
  variant_name: ppo_place_code_north
