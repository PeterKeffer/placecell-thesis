defaults: [../../downstream/wallgap_navigation_ppo]
name: ppo_coordinates_north
observation:
  feature_sources: [current_position_xy_scaled, goal_xy_scaled, heading_sin_cos]
training:
  learning_rate: 0.0005
