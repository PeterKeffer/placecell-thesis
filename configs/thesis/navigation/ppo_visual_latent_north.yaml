defaults: [../../downstream/wallgap_navigation_ppo]
name: ppo_visual_latent_north
models:
  place_model_artifact_id: tag:baseline
observation:
  feature_sources: [ae_latent, goal_xy_scaled, heading_sin_cos]
training:
  ent_coef: 0.003
