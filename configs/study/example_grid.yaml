name: example_grid
sweep:
  method: grid
  base_experiment: smoke_jaxenstein
  parameters:
    spatial_model.sparsifier.k_fraction: [0.05, 0.1]
    policies.artifact_reuse: reuse_if_config_match
  seeds: [42]
tracking:
  use_wandb: false
  run_root: smoke/runs
  artifact_root: smoke/artifacts
  study_name: example_grid
  variant_name: grid
